# Core dependency - installing this pulls in nearly everything else needed:
# ax-platform, emcee, gitpython, jupyterlab, matplotlib, numpy, openpyxl,
# pandas, pymoo, pyodbc, pySIMsalabim, scikit-optimize, scipy, seaborn, tk,
# torch, torchaudio, torchvision (botorch/gpytorch come along transitively
# via ax-platform). See requirements-frozen.txt for the exact full pinned
# environment (pip freeze from the `optimpv` pyenv env).
optimpv==1.4

# Imported directly by the fitting/MCMC scripts, not part of optimpv's own
# dependency tree:
httpimport

# vendor/optimpv_github/ (your own vendored code, see README.md) may need
# additional packages beyond the above - check with:
#   grep -rhE "^import|^from" vendor/optimpv_github | sort -u
# and add anything missing here.

# NOT required to reproduce the analysis - only needed if you have the
# original lab hardware and want to re-run the acquisition script
# (src/measurement/250121-trPL_PowerDeps-MeasProcedure.py):
#   PyDAQmx, ni_pulser_loop_new, snAPI, pylablib, TLPM
